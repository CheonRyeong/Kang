
<!doctype html>
<html lang="en">
<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <title>Hello, world!</title>
</head>
<body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
{{>layout/header}}
{{#article}}
    <div class="container-fluid py-4">
        <div class="row justify-content-center">
            <!-- 폭을 넓게: xl에서 10, xxl에서 8 컬럼 -->
            <div class="col-12 col-lg-11 col-xl-10 col-xxl-8">
                <div class="card shadow-sm border-0">
                    <div class="card-body p-4 p-md-5">

                        <!-- 헤더 -->
                        <div class="d-flex align-items-center justify-content-between mb-3">
                            <span class="badge bg-secondary">#{{id}}</span>
                            <small class="text-muted">작성자: {{author}}</small>
                        </div>

                        <!-- 제목 -->
                        <h1 class="h3 h2-md mb-4">{{title}}</h1>

                        <hr class="mb-4"/>

                        <!-- 본문: 줄바꿈/긴 줄 유지 -->
                        <div class="fs-5" style="white-space: pre-wrap; line-height: 1.7;">
                            {{content}}
                        </div>

                        <hr class="my-4"/>

                        <!-- 액션 버튼 -->
                        <div class="d-flex gap-2">
                            <a href="/articles/{{id}}/edit" class="btn btn-primary">수정</a>
                            <form method="post" action="/articles/{{id}}/delete" onsubmit="return confirm('정말 삭제할까요?');">
                                <button type="submit" class="btn btn-danger">삭제</button>
                            </form>

                            <a href="../articles" class="btn btn-outline-secondary">목록</a>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
{{/article}}
{{>layout/footer}}
</body>
</html>